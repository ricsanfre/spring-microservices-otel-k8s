apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: infra-monitoring-config
  namespace: flux-system
spec:
  dependsOn:
    - name: infra-monitoring-app
    - name: infra-security-config   # ESO ClusterSecretStore required for ExternalSecret
  interval: 10m
  path: ./gitops/infrastructure/environments/staging/monitoring/config
  sourceRef:
    kind: GitRepository
    name: flux-system
  prune: true
  postBuild:
    substituteFrom:
      - kind: ConfigMap
        name: cluster-settings
