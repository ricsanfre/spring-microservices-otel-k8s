apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# valkey staging overlay — no overrides, passes base through unchanged.
#
# Apply:
#   kubectl apply -k gitops/infrastructure/valkey/overlays/staging
resources:
  - ../../base
