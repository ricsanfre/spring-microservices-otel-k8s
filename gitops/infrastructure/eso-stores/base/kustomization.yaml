apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# ESO base — HelmRepository + HelmRelease for Flux Helm controller.
# eso-values.yaml is superseded by spec.values in helmrelease.yaml.
resources:
  - helmrepository.yaml
  - helmrelease.yaml
