apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: cert-manager
# Staging overlay — includes the HelmRelease/OCIRepository base unchanged.
# Add environment-specific patches below when needed.
resources:
  - ../../base
