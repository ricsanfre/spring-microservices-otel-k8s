apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# cert-manager base — HelmRepository + HelmRelease for Flux Helm controller.
# cert-manager-values.yaml is superseded by spec.values in helmrelease.yaml.
resources:
  - namespace.yaml
  - helmrepository.yaml
  - helmrelease.yaml
