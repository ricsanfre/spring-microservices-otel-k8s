apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: cert-manager
# Staging environment overlay for config CRs.
# Includes the base CRs unchanged; add environment-specific patches below when needed.
# Targeted directly by the corresponding Flux Kustomization (Layer 2, dependsOn: infra-operators).
resources:
  - ../../base
