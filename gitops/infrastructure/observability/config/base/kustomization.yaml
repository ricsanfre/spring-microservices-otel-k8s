apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Observability config base — CRs that require OTel Operator CRDs and ESO.
# Included by environment overlays (config/staging, config/production).
namespace: monitoring
resources:
  - collector.yaml
