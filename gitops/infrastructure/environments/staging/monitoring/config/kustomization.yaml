apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# monitoring/config — Layer 2 for the monitoring group.
# Applies the OTel Collector CR and Grafana admin ExternalSecret.
# Requires:
#   - OTel Operator CRDs (infra-monitoring-app healthChecks)
#   - ESO ClusterSecretStore (infra-security-config) for the ExternalSecret
# Targeted by infra-monitoring-config Flux Kustomization
# (dependsOn: infra-monitoring-app + infra-security-config).
resources:
  - ../../../../observability/config/overlays/staging
