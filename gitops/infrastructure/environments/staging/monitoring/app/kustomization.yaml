apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# monitoring/app — Layer 1 for the monitoring group.
# Installs the full observability operator stack via Helm:
#   - kube-prometheus-stack (Prometheus + Grafana)
#   - OpenTelemetry Operator
#   - Grafana Tempo (monolithic)
#   - Grafana Loki (SingleBinary)
# Targeted by infra-monitoring-app Flux Kustomization.
resources:
  - ../../../../observability/app/overlays/staging
