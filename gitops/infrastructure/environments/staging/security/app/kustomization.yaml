apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# security/app — Layer 1 for the security group.
# Installs External Secrets Operator Helm chart (installs ESO CRDs).
# Targeted by infra-security-app Flux Kustomization.
resources:
  - ../../../../eso-stores/app/overlays/staging
