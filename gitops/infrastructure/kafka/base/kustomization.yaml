apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# kafka base — OCIRepository + HelmRelease for Flux Helm controller.
# strimzi-operator-values.yaml is superseded by spec.values in helmrelease.yaml.
resources:
  - ocirepository.yaml
  - helmrelease.yaml
