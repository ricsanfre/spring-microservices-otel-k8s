apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Kafka config base — CRs that require Strimzi Operator CRDs.
# Included by environment overlays (config/staging, config/production).
resources:
  - cluster.yaml
