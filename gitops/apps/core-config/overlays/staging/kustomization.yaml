apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# core-config staging overlay — no overrides, passes base through unchanged.
#
# Apply:
#   kubectl apply -k gitops/apps/core-config/overlays/staging
resources:
  - ../../base
