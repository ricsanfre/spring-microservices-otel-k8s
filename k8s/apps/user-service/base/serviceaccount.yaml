---
# ServiceAccount used by user-service pods.
# Kubernetes DiscoveryClient (Spring Cloud Kubernetes) uses this account to
# watch Services, Endpoints, and Pods for load-balanced calls between microservices.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: user-service
  namespace: e-commerce
