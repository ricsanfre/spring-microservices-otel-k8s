apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# cert-manager — ClusterIssuers + wildcard TLS certificate
# Apply with: kubectl apply -k k8s/infra/cert-manager
resources:
  - cluster-issuer.yaml
  - wildcard-certificate.yaml
