# Envoy Gateway — GatewayClass
# References the controller installed via the envoy-gateway Helm chart.
---
apiVersion: gateway.networking.k8s.io/v1
kind: GatewayClass
metadata:
  name: eg
spec:
  controllerName: gateway.envoyproxy.io/gatewayclass-controller
